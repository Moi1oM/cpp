# 00_real_basic - 진짜 기초, 정말로 제로에서 시작하는 C++ 문법 정복

## ✅ 문법 완벽 정복 체크리스트 (Day-by-Day)

### Week 1: 기본 → 제어문 → 함수
- [ ] **Day 1: 컴파일/실행 과정**
  - [ ] `g++ -v hello.cpp`로 4단계(preprocess, compile, assemble, link) 확인
  - [ ] `CMakeLists.txt` 10줄 작성 → `cmake . && make`
  - [ ] `sizeof(int)=4, sizeof(double)=8, sizeof(long long)=8` 출력 확인

- [ ] **Day 2: 변수/연산자/입출력**
  - [ ] `int i=5; ++i vs i++` 결과 다름 확인 (pre vs post)
  - [ ] `printf("%d", x)` vs `cout << x` → 100만번 출력 5배 차이 측정
  - [ ] `putchar('A')` 100만번 → `cout` 압살 숫자 기록

- [ ] **Day 3: 제어문 완벽**
  - [ ] `switch` vs `if-else` 100만번 분기 → switch 20% 빠름 확인
  - [ ] `for(int i=0; i<1000000; i++)` vs `range-based for` 속도 비교
  - [ ] `break/continue` 3가지 케이스 완벽 이해

- [ ] **Day 4: 함수 전달 3가지**
  - [ ] `call by value` → 복사 발생 증명 (struct 큰거 전달)
  - [ ] `call by reference(&)` vs `call by pointer(*)` 어셈블리 Godbolt 비교
  - [ ] `inline` 함수 실제 최적화 안되는 경우 찾기

- [ ] **Day 5: 배열=포인터 증명**
  - [ ] `int a[10]; printf("%p %p", a, &a[0]);` → 주소 동일 출력
  - [ ] `a[5] = *(a+5)` 직접 써보기
  - [ ] 배열 경계 초과 → 세그폴트 직접 발생 → 디버그로 위치 찾기

- [ ] **Day 6: 문자열 전쟁**
  - [ ] `char buf[100]; strcpy(buf, "hello");` 버퍼 오버플로우 발생
  - [ ] `std::string` vs `char[] + strlen` 속도 → C가 3배 빠름 확인
  - [ ] `const char*` vs `char* const` vs `const char* const` 3가지 테스트

### Week 2: 실습 프로젝트 → 검증
- [ ] **Day 7: 에라토스테네스 체 (n=1e7, 0.1초 목표)**
  - [ ] `bool prime[10000000]` 스택 오버플로우 → `new bool[]`로 힙 이동
  - [ ] `memset(prime, true, sizeof(prime))` 사용

- [ ] **Day 8: 문자열 뒤집기 5가지**
  - [ ] 반복자 / 재귀 / `std::reverse` / 포인터 swap / `memmove`
  - [ ] 100만자 → `std::reverse` 최강 증명

- [ ] **Day 9: 계산기 (사칙+괄호)**
  - [ ] `std::stack` 2개 (숫자/연산자)로 완벽 파싱
  - [ ] `"3*(4+2)" → 18` 정확히 계산

- [ ] **Day 10: 틱택토 콘솔**
  - [ ] `char board[3][3]` → 승리판정 8방향 함수
  - [ ] 플레이어 교대 + 무승부 처리 완성

- [ ] **Day 11: 주식 틱 파일 처리**
  - [ ] CSV 파싱 → `double price, long long volume`
  - [ ] 1GB 파일 2초 내 평균/최대값 계산

- [ ] **Day 12: CMake 전 프로젝트 통합**
  - [ ] `add_executable(day1 ...)`, `add_executable(day2 ...)` 7개
  - [ ] `make all` 한방에 빌드

- [ ] **Day 13: Valgrind 완전 검증**
  - [ ] `valgrind ./day1` → `no leaks` 100%
  - [ ] 의도적 누수 만들기 → `valgrind`로 정확히 위치 잡기

- [ ] **Day 14: 최종 리뷰 + Presto 문법 퀴즈**
  - [ ] `printf("스택에 뭐가 올라가죠?");` → 함수 지역변수 답변
  - [ ] `printf("힙은 누가 해제?");` → 프로그래머 직접 delete
  - [ ] 모든 `sizeof` 값 암기 + `++i vs i++` 설명 가능

## 🚀 완성 증명 기준


**이 폴더는 이름 그대로 "진짜 기초"입니다.**  
Quant Dev를 목표로 하는 너가 **이미 01_basics로 바로 뛰어들면 100% 좌절한다.**  
내가 지난 3년간 봐온 수많은 지원자들 중에 "C++ 문법은 알죠~" 하면서도  
`int* p = new int[10];` 쓰고 delete 안 해서 메모리 누수 내는 애들이 태반이었다.

여기는 **절대 건너뛰지 마라.**  
여기서 2주만 제대로 해도  
나중에 01_basics ~ 99_quant_prep까지 한 번에 쭉 뚫린다.

### 이 폴더의 철학
- **quant는 "기본이 탄탄한 애"를 원한다**
- 저지연 트레이딩 시스템은 "문법 실수 하나 = 수억 손실"이다
- 여기서 실수해도 괜찮아. 오히려 여기서 다 틀려봐야 나중에 안 틀린다

### 학습 목표 (2주 안에 끝내기)

1. **컴파일 → 실행 과정 완전히 이해**
   - .cpp → .o → 실행파일 과정 직접 보기 (g++ -v 옵션으로)
   - CMake 최소한 하나 만들어보기 (CMakeLists.txt 10줄)

2. **기본 문법 100% 암기 수준**
   - 변수 선언, 초기화 (int, double, char, bool)
   - 기본 타입 크기 정확히 외우기 (sizeof로 확인 필수)
   - 연산자 우선순위 (특히 ++i vs i++ 실수하면 나중에 atomic에서 죽음)
   - 입출력 (cin/cout vs scanf/printf 속도 비교 필수 → printf가 3~5배 빠름)

3. **제어문 완벽 장악**
   - if / else if / switch (switch가 더 빠른 경우 실측)
   - for / while / do-while (범위 기반 for문도 여기서 미리 맛보기)
   - break, continue, goto (goto는 절대 쓰지 말라고 배우는 곳)

4. **함수 완벽 이해**
   - call by value vs call by reference vs call by pointer
   - 인라인 함수 (inline 키워드 실측 (최적화 안 될 때도 많음)
   - 함수 오버로딩 vs 템플릿 (여기서부터 미리 차이 느끼기)

5. **배열과 포인터의 첫 만남 (진짜 중요)**
   - 배열은 사실 포인터라는 걸 눈으로 확인 (int a[10];과 int* p = a; 동일)
   - 배열 경계 넘어가서 세그폴트 직접 일으켜보기 (Valgrind 없이도 잡기)
   - const char* vs char* const vs const char* const 차이 정확히

6. **문자열 다루기**
   - C 스타일 char[] vs std::string (std::string이 90% 경우 더 느림)
   - strcpy, strcat 금지 사유 직접 체험 (버퍼 오버플로우 일으켜보기)

### 실습 과제 (이거 안 하면 의미 없음)

매일 하나씩 무조건 만들기:

Day 1: Hello World → 1000000번 출력 속도 측정 (cout vs printf)
Day 2: 1부터 n까지 소수 찾기 (에라토스테네스 체 구현)
Day 3: 문자열 뒤집기 5가지 방법 (반복자, 재귀, std::reverse 등)
Day 4: 간단한 계산기 (사칙연산 + 괄호 처리)
Day 5: 틱택토 게임 콘솔 버전 (2차원 배열 + 함수 분리)
Day 6: 파일 입출력으로 주식 틱 데이터 읽어서 평균 계산
Day 7: 위 실습들 전부 CMake로 빌드 + Valgrind로 메모리 누수 검사

### quant 현실 연결
2025년 입사자 중 한 명이 전화면접에서 떨어진 이유:  
"printf와 cout 차이 아나요?" → "cout이 더 안전하죠"라고만 답함  
→ 실제로 printf가 5배 빠름을 숫자로 못 보여줘서 탈락

**여기서 cout만 쓰던 습관 들이면 나중에 low-latency에서 죽는다.**  
printf, putchar, fwrite부터 익혀라.

### 추천 설정 (지금 당장 해라)
```bash
# .bashrc나 .zshrc에 추가
alias gpp="g++ -Wall -Wextra -Werror -O2 -std=c++20"
alias dbg="g++ -g -Wall -Wextra -std=c++20"
alias val="valgrind --leak-check=full --show-leak-kinds=all"


00_real_basic/
├── README.md
├── 01_compile_process/     # 컴파일 과정 이해
│   ├── hello.cpp
│   ├── CMakeLists.txt
│   └── compile_steps.sh
├── 02_basic_syntax/        # 변수/연산자/입출력
│   ├── types_sizes.cpp
│   ├── operators.cpp
│   └── io_benchmark.cpp
├── 03_control_flow/        # 제어문 완벽 장악
│   ├── switch_vs_if.cpp
│   └── loops_comparison.cpp
├── 04_functions/           # 함수 전달 방식
│   ├── call_by_value_ref.cpp
│   └── inline_test.cpp
├── 05_arrays_pointers/     # 배열=포인터 증명
│   ├── array_bounds.cpp
│   └── const_pointers.cpp
├── 06_strings/             # C-string vs std::string
│   ├── buffer_overflow.cpp
│   └── string_perf.cpp
└── daily_projects/         # 7일 실습 과제
    ├── day1_hello_speed/
    ├── day2_sieve/
    ├── day3_reverse/
    ├── day4_calculator/
    ├── day5_tictactoe/
    ├── day6_stock_avg/
    └── day7_cmake_valgrind/
